import time

# ============================================================
# 1. BUBBLE SORT
# ============================================================

def bubble_sort(arr):
    n = len(arr)

    for i in range(n - 1):
        swapped = False

        for j in range(n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True

        if not swapped:
            break


# ============================================================
# 2. SELECTION SORT
# ============================================================

def selection_sort(arr):
    n = len(arr)

    for i in range(n - 1):
        min_index = i

        for j in range(i + 1, n):
            if arr[j] < arr[min_index]:
                min_index = j

        arr[i], arr[min_index] = arr[min_index], arr[i]


# ============================================================
# 3. INSERTION SORT
# ============================================================

def insertion_sort(arr):
    n = len(arr)

    for i in range(1, n):
        key = arr[i]
        j = i - 1

        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1

        arr[j + 1] = key


# ============================================================
# 4. MERGE SORT
# ============================================================

def merge_sort(arr):

    if len(arr) > 1:

        mid = len(arr) // 2

        left = arr[:mid]
        right = arr[mid:]

        # Recursively sort both halves
        merge_sort(left)
        merge_sort(right)

        i = 0
        j = 0
        k = 0

        # Merge the sorted halves
        while i < len(left) and j < len(right):

            if left[i] < right[j]:
                arr[k] = left[i]
                i += 1
            else:
                arr[k] = right[j]
                j += 1

            k += 1

        # Copy remaining elements from left
        while i < len(left):
            arr[k] = left[i]
            i += 1
            k += 1

        # Copy remaining elements from right
        while j < len(right):
            arr[k] = right[j]
            j += 1
            k += 1


# ============================================================
# 5. QUICK SORT
# ============================================================

def quick_sort(arr):

    if len(arr) <= 1:
        return arr

    # Select pivot
    pivot = arr[len(arr) // 2]

    # Divide array into three parts
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]

    # Recursively sort
    return quick_sort(left) + middle + quick_sort(right)


# ============================================================
# MAIN PROGRAM
# ============================================================

# User Input
arr = list(map(int, input("Enter numbers separated by spaces: ").split()))

print("\nOriginal Array:", arr)


# ============================================================
# BUBBLE SORT
# ============================================================

bubble_arr = arr.copy()

start_time = time.perf_counter()
bubble_sort(bubble_arr)
end_time = time.perf_counter()

print("\n--- Bubble Sort ---")
print("Sorted Array:", bubble_arr)
print("Execution Time: {:.8f} seconds".format(end_time - start_time))
print("Best Case: O(n)")
print("Average Case: O(n^2)")
print("Worst Case: O(n^2)")
print("Space Complexity: O(1)")


# ============================================================
# SELECTION SORT
# ============================================================

selection_arr = arr.copy()

start_time = time.perf_counter()
selection_sort(selection_arr)
end_time = time.perf_counter()

print("\n--- Selection Sort ---")
print("Sorted Array:", selection_arr)
print("Execution Time: {:.8f} seconds".format(end_time - start_time))
print("Best Case: O(n^2)")
print("Average Case: O(n^2)")
print("Worst Case: O(n^2)")
print("Space Complexity: O(1)")


# ============================================================
# INSERTION SORT
# ============================================================

insertion_arr = arr.copy()

start_time = time.perf_counter()
insertion_sort(insertion_arr)
end_time = time.perf_counter()

print("\n--- Insertion Sort ---")
print("Sorted Array:", insertion_arr)
print("Execution Time: {:.8f} seconds".format(end_time - start_time))
print("Best Case: O(n)")
print("Average Case: O(n^2)")
print("Worst Case: O(n^2)")
print("Space Complexity: O(1)")


# ============================================================
# MERGE SORT
# ============================================================

merge_arr = arr.copy()

start_time = time.perf_counter()
merge_sort(merge_arr)
end_time = time.perf_counter()

print("\n--- Merge Sort ---")
print("Sorted Array:", merge_arr)
print("Execution Time: {:.8f} seconds".format(end_time - start_time))
print("Best Case: O(n log n)")
print("Average Case: O(n log n)")
print("Worst Case: O(n log n)")
print("Space Complexity: O(n)")


# ============================================================
# QUICK SORT
# ============================================================

start_time = time.perf_counter()
quick_arr = quick_sort(arr.copy())
end_time = time.perf_counter()

print("\n--- Quick Sort ---")
print("Sorted Array:", quick_arr)
print("Execution Time: {:.8f} seconds".format(end_time - start_time))
print("Best Case: O(n log n)")
print("Average Case: O(n log n)")
print("Worst Case: O(n^2)")
print("Space Complexity: O(n)")


# ============================================================
# COMPARISON TABLE
# ============================================================

print("\n============================================================")
print("              SORTING ALGORITHM COMPARISON")
print("============================================================")

print("\nAlgorithm        Best Case       Average Case      Worst Case")
print("------------------------------------------------------------")
print("Bubble Sort      O(n)            O(n^2)            O(n^2)")
print("Selection Sort   O(n^2)          O(n^2)            O(n^2)")
print("Insertion Sort   O(n)            O(n^2)            O(n^2)")
print("Merge Sort       O(n log n)      O(n log n)        O(n log n)")
print("Quick Sort       O(n log n)      O(n log n)        O(n^2)")

print("\n============================================================")
print("                    PROGRAM COMPLETED")
print("============================================================")